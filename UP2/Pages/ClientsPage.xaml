<Page x:Class="UP2.Pages.ClientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UP2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ClientsPage">

    <Grid>
        <DataGrid x:Name="DGridClients" AutoGenerateColumns="False" Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер клиента" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="Фамилия клиента" Binding="{Binding Surname}"/>
                <DataGridTextColumn Header="Имя клиента" Binding="{Binding FirstName}"/>
                <DataGridTextColumn Header="Отчество клиента" Binding="{Binding LastName}"/>
                <DataGridTextColumn Header="Номер телефона" Binding="{Binding PhoneNumber}"/>
                <DataGridTextColumn Header="Сумма к оплате" Binding="{Binding PaymentSum}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
